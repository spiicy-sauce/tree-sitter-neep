; Language injections for Neep.
;
; Neep has no embedded sub-languages at present (note and freeform text are
; opaque). This file is intentionally minimal and exists so consumers can
; rely on a stable query path.
